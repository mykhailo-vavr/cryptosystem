export type Locale = 'ua' | 'en';

export enum LocalesEnum {
  EN = 'en',
  UA = 'ua',
}

export enum ActionsEnum {
  ENCODE = 'encode',
  DECODE = 'decode',
}

export enum SaveAsEnum {
  FILE = 'file',
  TEXT = 'text',
}
